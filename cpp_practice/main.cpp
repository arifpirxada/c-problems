#include <iostream> // library for taking inputs and outputs

using namespace std; // Standard template library, we don't have to write std::cin, std::cout or the same for any other function when we include this line

// #include<bits/stdc++.h> This includes all the libraries that are present in c++

int main()
{
    // INPUT AND OUPUT IN C++ =>
    /*
    int x, y;

    cout << "Enter value of x & y:" << endl;
    cin >> x >> y;
    cout << "x: " << x << "; y: " << y;
    */


    // DATATYPES =>
    /*
    int age = 10;
    long cost = 999;
    long long more = 939;
    cout << "age: " << age << "; cost: " << cost << "; more: " << more;
    */

    /*
    float fl = 7.8;
    double dbl = 29.3;
    cout << "float: " << fl << "; Double: " << dbl;
    */

    /*
    string str;
    cout << "Enter the string (word): ";
    cin >> str; // str will only store one word from the input
    cout << "Your string: " << str << "\n";

    string line;
    cout << "Enter a string: ";
    getline(cin, line); // will store all words of first line
    cout << "Your string: " << line;

    char c = 'a'; // takes one letter. use single quotes ('')
    */

    // IF ELSE & SWITCH STATEMENT =>
    /*
    int age;
    cout << "Enter your age: ";
    cin >> age;

    if (age > 18) {
        cout << "You are an adult!";
    } else if (age >= 13) {
        cout << "You are a teenager!";
    } else {
        cout << "You are a kid!";
    }
    */

    return 0;
}