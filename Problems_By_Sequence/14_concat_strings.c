#include <stdio.h>

// Program to accept two strings and concatenate them i.e. The second string is
// appended to the end of the first string

int main()
{
    char first_string[25], second_string[25];
    printf("Enter two strings:\n");
    scanf("%d%d", &first_string, &second_string);
    
    return 0;
}